# Long, fine-grained run (matches the second training run from the notebook).
data: data/one_traj_loss_check.npy
out_dir: runs/run_long

features: [64, 64, 64, 64, 1]
dt: 0.03125
dx: 0.4

epochs: 666
lr: 2.0e-3
early_stop_patience: 160
lr_patience: 150
tol: 1.0e-10

margin: 16
window_t: 80
window_x: 180
num_samples: 16
L: 6
rollout_steps: 9
num_draws: 6

use_smooth: false
lambda_smooth: 1.0e-4
seed: 0
